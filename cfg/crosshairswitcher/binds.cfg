bind 			1			weapon1

bind 			2			weapon2

bind 			3			weapon3

bind 			q			lastweapon

bind			MWHEELUP		previousweapon

bind			MWHEELDOWN		nextweapon

bind 			\			+quickswitchmenu

///////////////////////////////////////////////////////

//bind		MWHEELUP		weapon1

//bind		MWHEELDOWN		weapon2

//bind 		MOUSE5		weapon3

///////////////////////////////////////////////////////

//bind		q			switch_between_slot1_and_slot2


//////////////////////////////////////////////////////////
// Gemmellness's shit					//
//////////////////////////////////////////////////////////

// This stuff toggles viewmodels with a single key, whether they're on
// for the current slot or not. 

// Won't work perfectly if you use
// different viewmodel_fovs for each slot, but feel free to try it.
// Also note that this shit breaks for viewmodel fovs off_medic and
// off_with_muzzleflash

// Uncomment the lines starting with alias if you want to use.

// Bind each of these to the same key, they toggle viewmodels.
// Binding them to seperate keys means you would have a different key
// toggle viewmodels depending on the normal state for that slot.
// alias togglevm "bind 			t 		v"
//alias togglevm2 "bind 			t 		v2"

// This is the fov that you toggle to and from.
// Change only the number, not the va or vb.
//alias "v"  				va90		//va[fov]
//alias "v2"				vb90		//vb[fov]
tf_remember_lastswitched "0"
tf_remember_activeweapon "0"